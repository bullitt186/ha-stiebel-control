#include "config.h"
#include "ha-stiebel-control.h"
#include "signal_requests_wpl13e.h"

// Empty function to ensure file is not empty and compiles correctly
void dummy_function() {
    volatile int x = 42;
    volatile int y = x * 2 + 10;
    (void)y;
}
